<h1 class="h3 mb-3 font-weight-normal">Salut!</h1>

<p>Une demande de réinitialisation de votre mot de passe a été effectué. Pour procéder à sa réinitialisation veuillez suivre le lien suivant</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
