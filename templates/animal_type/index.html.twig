{% extends 'base.html.twig' %}

{% block title %}AnimalType index{% endblock %}

{% block body %}
    <h1>AnimalType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for animal_type in animal_types %}
            <tr>
                <td>{{ animal_type.id }}</td>
                <td>{{ animal_type.name }}</td>
                <td>
                    <a href="{{ path('app_animal_type_show', {'id': animal_type.id}) }}">show</a>
                    <a href="{{ path('app_animal_type_edit', {'id': animal_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_animal_type_new') }}">Create new</a>
{% endblock %}
