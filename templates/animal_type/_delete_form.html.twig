<form method="post" action="{{ path('app_animal_type_delete', {'id': animal_type.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce type d'animal?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal_type.id) }}">
    <div class="text-center">
        <button class="btn">Supprimer</button>
    </div>
</form>
