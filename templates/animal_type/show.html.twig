{% extends 'base.html.twig' %}

{% block title %}AnimalType{% endblock %}

{% block body %}
    <h1>AnimalType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animal_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_type_index') }}">back to list</a>

    <a href="{{ path('app_animal_type_edit', {'id': animal_type.id}) }}">edit</a>

    {{ include('animal_type/_delete_form.html.twig') }}
{% endblock %}
