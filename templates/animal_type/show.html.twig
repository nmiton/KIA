{% extends 'base.html.twig' %}

{% block title %}Type d'animaux{% endblock %}
{% block main_id %}animal_type{% endblock %}

{% block body %}
    <h1>Type d'animaux</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ animal_type.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ animal_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_type_index') }}">Retour</a>

    <a href="{{ path('app_animal_type_edit', {'id': animal_type.id}) }}">Éditer</a>

    {{ include('animal_type/_delete_form.html.twig') }}
{% endblock %}
